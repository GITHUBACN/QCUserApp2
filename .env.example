# Copy to .env and set values
AWS_PROFILE=QC-team-app
PROJECT1_ARN=project1_arn
MODEL1_ARN=model1_arn
VERSION_NAME1=version_name1
PROJECT2_ARN=project2_arn
MODEL2_ARN=model2_arn
VERSION_NAME2=version_name2
MIN_INFERENCE_UNITS=1

# Optional text-reading / Bedrock config
# Either set TEXT_READING_PROMPT directly (use \n for newlines) or point to a file.
#TEXT_READING_PROMPT=...
#TEXT_READING_PROMPT_FILE=.env.text_prompt.txt
BEDROCK_REGION=us-east-2
BEDROCK_MODEL_ID=bedrock_model_id
AWS_BEARER_TOKEN_BEDROCK=aws_bearer_token_bedrock
