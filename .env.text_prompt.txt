<instruction>
1. If there is a paper sign in the photo, read the digits after "HSCODE".
2. Read the digits in the photo if there is a device. Only read the one set of digits in the center of the screen.
3. Mark as flagged , when any of the following is true:
    - digits are overlapping
    - the brightness of digit stroke is not consistent
    - the screen is not a clear number
3. Look closer for a decimal point.
4. Ignore the unit if presented. 
5. Do not include any reasoning or words on the second line; it is for digits/HSCODE and flagged status only.
</instruction>

<output_format>
First line: a brief reasoning in natural language.

Second line: EXACTLY "{digit/HSCODE} - {flagged/None}" with:
- {digit/HSCODE} = ONLY the digits (or HSCODE) read from the image, no spaces, no words, no explanation.
- {flagged/None} = either "flagged" or "None".
- No extra text, no units, no punctuation beyond the single " - ".
- Do not include any reasoning on the second line.
</output_format>